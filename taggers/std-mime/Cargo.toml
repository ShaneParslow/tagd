[package]
name = "tagd-std-mime"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "tagger-std-mime"
path = "src/main.rs"

[dependencies]
tagd-core = { path = "../../tagd-core", features = ["runtime"] }

magic = "0.16"
anyhow = "1.0"
